spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interaction_fields?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 276364092
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        sentinel:
    transport:
      dashboard: 127.0.0.1:8858
  zipkin:
    base-url: http://127.0.0.1:9411/
    discoveryClientEnabled: false
  sleuth:
    sampler:
      probability: 1.0 # 采样百分比
feign:
  sentinel:
    enabled: true
  client:
    config:
      default:
        connect-timeout: 2000
        read-timeout: 5000
logging:
  level:
    root: error
    org.ktorm: debug
